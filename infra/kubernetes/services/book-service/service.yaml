---
apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: library
  labels:
    app: book-service
spec:
  type: ClusterIP
  selector:
    app: book-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
